#include <stdio.h>
#include <ctype.h>

int main(){
    char accept;
    accept = getchar();
    if (isdigit (accept)== 1)
        printf ("first is true");
    
    else
        printf ("first is true");
    
    return 0;
}
//if it returns value as 1 it is true