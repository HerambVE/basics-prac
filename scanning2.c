#include <stdio.h>

void main()
{
    char s[15];
    scanf("%s", s);
    printf("%s", s);
}

//to store a string you definitely need an array
//you do not need ampersand when taking input from as a string
//always keep format specifiers same or they encounter an error